#include <stdio.h>
/**
 * main - Print 0-9 only ussing putchar command
 *
 * Return: 0 Always (Success)
 */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
	putchar(n);
	}
	putchar('\n');
		return (0);
}
